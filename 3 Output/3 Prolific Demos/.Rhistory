getwd()
####Combine .csv files into master datasheet####
setwd("C:/Users/nickm/OneDrive/Documents/GitHub/BOI-Norms/3 Output/3 Prolific Demos/Data")
#Get the files names
files = list.files(pattern = "*.csv")
#read everything in and merge into one dataframe.
dat = do.call(rbind, lapply(files, function(x) read.csv(x, stringsAsFactors = FALSE)))
####Combine .csv files into master datasheet####
setwd("C:/Users/nickm/OneDrive/Documents/GitHub/BOI-Norms/3 Output/3 Prolific Demos/Data")
#Get the files names
files = list.files(pattern = "*.csv")
#read everything in and merge into one dataframe.
dat = do.call(rbind, lapply(files, function(x) read.csv(x, stringsAsFactors = FALSE)))
#Get the files names
files = list.files(pattern = "*.csv")
#read everything in and merge into one dataframe.
dat = do.call(rbind, lapply(files, function(x) read.csv(x, stringsAsFactors = FALSE)))
setwd("..")
####Combine .csv files into master datasheet####
setwd("C:/Users/nickm/OneDrive/Documents/GitHub/BOI-Norms/3 Output/3 Prolific Demos/Data")
#Get the files names
files = list.files(pattern = "*.csv")
#read everything in and merge into one dataframe.
dat = do.call(rbind, lapply(files, function(x) read.csv(x, stringsAsFactors = FALSE)))
#Get the files names
files = list.files(pattern = "*.csv")
#read everything in and merge into one dataframe.
dat = do.call(rbind, lapply(files, function(x) read.csv(x, stringsAsFactors = FALSE)))
####Combine .csv files into master datasheet####
setwd("C:/Users/nickm/OneDrive/Documents/GitHub/BOI-Norms/3 Output/3 Prolific Demos/Data")
#Get the files names
files = list.files(pattern = "*.csv")
#read everything in and merge into one dataframe.
dat = do.call(rbind, lapply(files, function(x) read.csv(x, stringsAsFactors = FALSE)))
####Combine .csv files into master datasheet####
setwd("C:/Users/nickm/OneDrive/Documents/GitHub/BOI-Norms/3 Output/3 Prolific Demos/Data")
#Get the files names
files = list.files(pattern = "*.csv")
#read everything in and merge into one dataframe.
dat = do.call(rbind, lapply(files, function(x) read.csv(x, stringsAsFactors = FALSE)))
####Combine .csv files into master datasheet####
setwd("C:/Users/nickm/OneDrive/Documents/GitHub/BOI-Norms/3 Output/3 Prolific Demos/Data")
#Get the files names
files = list.files(pattern = "*.csv")
#read everything in and merge into one dataframe.
dat = do.call(rbind, lapply(files, function(x) read.csv(x, stringsAsFactors = FALSE)))
####Combine .csv files into master datasheet####
setwd("C:/Users/nickm/OneDrive/Documents/GitHub/BOI-Norms/3 Output/3 Prolific Demos/Data")
#Get the files names
files = list.files(pattern = "*.csv")
#read everything in and merge into one dataframe.
dat = do.call(rbind, lapply(files, function(x) read.csv(x, stringsAsFactors = FALSE)))
#Get the files names
files = list.files(pattern = "*.csv")
#read everything in and merge into one dataframe.
dat = do.call(rbind, lapply(files, function(x) read.csv(x, stringsAsFactors = FALSE)))
#Get the files names
files = list.files(pattern = "*.csv")
#read everything in and merge into one dataframe.
dat = do.call(rbind, lapply(files, function(x) read.csv(x, stringsAsFactors = FALSE)))
#Get the files names
files = list.files(pattern = "*.csv")
#read everything in and merge into one dataframe.
dat = do.call(rbind, lapply(files, function(x) read.csv(x, stringsAsFactors = FALSE)))
#Get the files names
files = list.files(pattern = "*.csv")
#read everything in and merge into one dataframe.
dat = do.call(rbind, lapply(files, function(x) read.csv(x, stringsAsFactors = FALSE)))
#Get the files names
files = list.files(pattern = "*.csv")
#read everything in and merge into one dataframe.
dat = do.call(rbind, lapply(files, function(x) read.csv(x, stringsAsFactors = FALSE)))
#Get the files names
files = list.files(pattern = "*.csv")
#read everything in and merge into one dataframe.
dat = do.call(rbind, lapply(files, function(x) read.csv(x, stringsAsFactors = FALSE)))
#Get the files names
files = list.files(pattern = "*.csv")
#read everything in and merge into one dataframe.
dat = do.call(rbind, lapply(files, function(x) read.csv(x, stringsAsFactors = FALSE)))
#Get the files names
files = list.files(pattern = "*.csv")
#read everything in and merge into one dataframe.
dat = do.call(rbind, lapply(files, function(x) read.csv(x, stringsAsFactors = FALSE)))
#Get the files names
files = list.files(pattern = "*.csv")
#read everything in and merge into one dataframe.
dat = do.call(rbind, lapply(files, function(x) read.csv(x, stringsAsFactors = FALSE)))
####Combine .csv files into master datasheet####
setwd("C:/Users/nickm/OneDrive/Documents/GitHub/BOI-Norms/3 Output/3 Prolific Demos/Data")
#Get the files names
files = list.files(pattern = "*.csv")
#read everything in and merge into one dataframe.
dat = do.call(rbind, lapply(files, function(x) read.csv(x, stringsAsFactors = FALSE)))
#Get the files names
files = list.files(pattern = "*.csv")
#read everything in and merge into one dataframe.
dat = do.call(rbind, lapply(files, function(x) read.csv(x, stringsAsFactors = FALSE)))
#Get the files names
files = list.files(pattern = "*.csv")
#read everything in and merge into one dataframe.
dat = do.call(rbind, lapply(files, function(x) read.csv(x, stringsAsFactors = FALSE)))
#Get the files names
files = list.files(pattern = "*.csv")
#read everything in and merge into one dataframe.
dat = do.call(rbind, lapply(files, function(x) read.csv(x, stringsAsFactors = FALSE)))
#Get the files names
files = list.files(pattern = "*.csv")
#read everything in and merge into one dataframe.
dat = do.call(rbind, lapply(files, function(x) read.csv(x, stringsAsFactors = FALSE)))
#Get the files names
files = list.files(pattern = "*.csv")
#read everything in and merge into one dataframe.
dat = do.call(rbind, lapply(files, function(x) read.csv(x, stringsAsFactors = FALSE)))
#Get the files names
files = list.files(pattern = "*.csv")
#read everything in and merge into one dataframe.
dat = do.call(rbind, lapply(files, function(x) read.csv(x, stringsAsFactors = FALSE)))
#Get the files names
files = list.files(pattern = "*.csv")
#read everything in and merge into one dataframe.
dat = do.call(rbind, lapply(files, function(x) read.csv(x, stringsAsFactors = FALSE)))
####Combine .csv files into master datasheet####
setwd("C:/Users/nickm/OneDrive/Documents/GitHub/BOI-Norms/3 Output/3 Prolific Demos/Data")
#Get the files names
files = list.files(pattern = "*.csv")
#read everything in and merge into one dataframe.
dat = do.call(rbind, lapply(files, function(x) read.csv(x, stringsAsFactors = FALSE)))
#Get the files names
files = list.files(pattern = "*.csv")
#read everything in and merge into one dataframe.
dat = do.call(rbind, lapply(files, function(x) read.csv(x, stringsAsFactors = FALSE)))
write.csv(dat, file = "Merged_demos_prolific.csv", row.names = F)
setwd("C:/Users/nickm/OneDrive/Documents/GitHub/BOI-Norms/3 Output/3 Prolific Demos")
dat = read.csv("Merged_demos_prolific.csv")
##what are we working with?
table(dat$Country.of.residence)
#we have 756 Prolific participants in the final dataset
dat = subset(dat,
dat$Country.of.residence != "CONSENT_REVOKED")
793 - 756
setwd("C:/Users/nickm/OneDrive/Documents/GitHub/BOI-Norms/3 Output/3 Prolific Demos")
setwd("C:/Users/nickm/OneDrive/Documents/GitHub/BOI-Norms/3 Output/3 Prolific Demos/Response")
#Get the files names
files = list.files(pattern = "*.csv")
#read everything in and merge into one dataframe.
dat2 = do.call(rbind, lapply(files, function(x) read.csv(x, stringsAsFactors = FALSE)))
write.csv(dat2, file = "Merged_responses.csv", row.names = F)
setwd("C:/Users/nickm/OneDrive/Documents/GitHub/BOI-Norms/3 Output/3 Prolific Demos")
##Read in data
dat = read.csv("Merged_demos_prolific.csv")
dat2 = read.csv("Merged_responses.csv")
##what are we working with?
table(dat$Country.of.residence)
#we have 756 Prolific participants in the final dataset
dat = subset(dat,
dat$Country.of.residence != "CONSENT_REVOKED")
#get the IDs for the Prolific participants in the final dataset
name_list = unique(dat2$Username)
View(dat2)
View(dat)
#do a merge?
dat3 = merge(dat, dat2, by.x = "Username")
#do a merge?
dat3 = merge(dat, dat2, by = "Username")
View(dat)
colnames(dat)[2]= "Username"
#we have 756 Prolific participants in the final dataset
dat = subset(dat,
dat$Country.of.residence != "CONSENT_REVOKED")
##Read in data
dat = read.csv("Merged_demos_prolific.csv")
dat2 = read.csv("Merged_responses.csv")
colnames(dat)[2]= "Username"
##what are we working with?
table(dat$Country.of.residence)
#we have 756 Prolific participants in the final dataset
dat = subset(dat,
dat$Country.of.residence != "CONSENT_REVOKED")
#get the IDs for the Prolific participants in the final dataset
name_list = unique(dat2$Username)
#do a merge?
dat3 = merge(dat, dat2, by = "Username")
#do a merge?
dat3 = merge(dat2, dat, by = "Username")
View(dat3)
length(unique(dat3$Username))
table(dat3$Country.of.residence)
table(dat3$Country.of.residence) / 30
View(dat)
UK = subset(dat3,
dat3$Country.of.residence == "United Kingdom")
length(unique(UK$Country.of.residence))
length(unique(UK$Username)
length(unique(UK$Username))
length(unique(UK$Username))
756 - 575
##what are we working with?
table(dat$Country.of.residence)
